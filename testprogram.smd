main:	ADDI $a0, $zero, 0x0005
	ADDI $a1, $zero, 0x0009
	ADDI $a2, $zero, 0x0002
	ADDI $t6, $zero, 0xFF
	ADDI $t7, $zero, 0xFF
	XOR $t5, $t6, $t7
	nop
	XOR $t5, $zero, $t6
	SLL $a2, $a2, 1
	SLL $a2, $a2, 1
	SRL $a2, $a2, 1
	SRL $a2, $a2, 1
	SUB $a3, $a3, $a2
	ADD $a2, $a1, $a0
	SUB $a3, $a1, $a0
	#SRA $a3, $a3, 1
	AND $t0, $a1, $a0
	OR $t1, $a1, $a0
	SLT $t2, $a0, $a1
	SW $a0, 4($zero)
	LW $t3, 4($zero)
	JAL test
	MULTU $a0, $a1
	MFHI $t5
	MFLO $t6
test:	ADDI $t4, $zero, 0x0007
	JR $ra